process.stdin.on('data', function (data)  {
    let num = Number(data.toString().trim()); 

    let R=[]
    for(i=0; i<=num; i++){
        R.push(i);
    }
    for (i=1; i<=R.length; update) {

     }
    process.exit(); 
  });